language: java
jdk: openjdk15

# Cache for speeding up the building
cache:
  directories:
    - $HOME/.m2

before_script:
  -cp ci/toolchains.xml $HOME/.m2/toolchains.xml

branches:
  only:
    - master
